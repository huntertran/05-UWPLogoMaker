<Page x:Class="UWPLogoMaker.View.FunctionGroup.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:converter="using:UWPLogoMaker.Utilities.Converter"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      DataContext="{Binding Source={StaticResource Locator},
                            Path=MainVm}"
      mc:Ignorable="d">

    <Page.Resources>
        <converter:NullToEnableConverter x:Key="NullToEnableConverter" />
        <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Frame x:Name="PreviewFrame" Grid.Row="0" />

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Pivot Title="Logo Background"
                   Grid.Row="0"
                   Grid.Column="0"
                   SelectionChanged="Pivot_SelectionChanged">
                <PivotItem Header="Color" />
                <!--<PivotItem Header="Gradient Color" />-->
                <PivotItem Header="Geometry" />
                <!--<PivotItem Header="Vector" />-->
            </Pivot>

            <Frame x:Name="BackgroundFrame"
                   Grid.Row="1"
                   Grid.Column="0" />

            <Button Grid.Row="0"
                    Grid.RowSpan="2"
                    Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Background="{StaticResource Brush5}"
                    Content="Generate"
                    IsEnabled="{Binding UserBitmap,
                                        Converter={StaticResource NullToEnableConverter}}"
                    Tapped="GenerateLogo_OnTapped" />
        </Grid>

        <Grid Grid.Row="0"
              Grid.RowSpan="2"
              Visibility="{Binding IsShowingProgress,
                                   Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.Background>
                <SolidColorBrush Opacity="0.8" Color="{StaticResource Color1}" />
            </Grid.Background>

            <ProgressRing Width="50"
                          Height="50"
                          Foreground="{StaticResource Brush5}"
                          IsActive="True" />

        </Grid>

    </Grid>
</Page>
